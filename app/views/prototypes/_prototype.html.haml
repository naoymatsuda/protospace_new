.col-sm-4.col-md-3.proto-content
  .thumbnail
    = link_to prototype_path(prototype) do
      = image_tag prototype.main_image.image
      .caption
        %h3
          = prototype.title
        .proto-meta
          .proto-user
            = prototype.user.name
          .proto-posted
            = prototype.created_at.strftime('%Y年%m月%d日')
        %ul.proto-tag-list.list-inline
          %li
            -if prototype.tag_list[0].present?
              .btn.btn-default
                = prototype.tag_list[0]
            -elsif prototype.tag_list[1].present?
              .btn.btn-default
                = prototype.tag_list[1]
            -else
